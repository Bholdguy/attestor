import { describe, it, expect } from "vitest";

// Step 0 placeholder so `npm run test` runs a green suite. Real unit tests
// (badge precedence, diff, identity, privilege, gate) arrive from Step 1 on.
describe("scaffold", () => {
  it("test runner is wired", () => {
    expect(1 + 1).toBe(2);
  });
});
