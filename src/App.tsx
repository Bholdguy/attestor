// Step 0 scaffold placeholder. The operator dashboard is built in Step 9
// (ARCHITECTURE.md §5 module map: RosterTable, WorkerTimeline, SnapshotDrawer,
// CompareView, CaseDetail, MetricsPanel, DemoPanel + DEMO DATA banner).
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 24 }}>
      <h1>Attestor</h1>
      <p>License Trust Supervisor — scaffold. Backend and UI are built per TASKS.md.</p>
    </main>
  );
}
